<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="nl.avans.kinoplex">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".presentation.activities.ManageListsActivity"></activity>
        <activity android:name=".presentation.activities.ReviewActivity" />
        <activity
            android:name=".presentation.activities.ChooseListPopUp"
            android:theme="@style/AppTheme.Popup" />
        <activity android:name=".presentation.activities.RegisterActivity" />
        <activity
            android:name=".presentation.activities.LoginActivity"
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.activities.DetailActivity" />
        <activity android:name=".presentation.activities.MainActivity" /> <!-- <activity android:name=".presentation.activities.ReviewActivity" /> -->
        <activity android:name=".presentation.activities.SearchActivity" />
        <activity android:name=".presentation.activities.ListActivity" />
        <activity android:name=".presentation.activities.AddReviewActivity" />
    </application>

</manifest>